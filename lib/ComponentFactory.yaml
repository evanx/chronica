
stores:

   environment:
      description: stores hostname

   service:
      description: stores services and their status, for alerts and reports


concat:
   packages:

defaults:
  componentStartTimeout: 5000
  componentEndTimeout: 5000
  loggerLevel: info
  test: {}
  env: {}
